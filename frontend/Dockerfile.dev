# Development Dockerfile for hot-reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies will be done via volume mount and docker-compose command
# This ensures compatibility with volume-mounted source code

# Expose port
EXPOSE 3000

# Start development server (overridden by docker-compose)
CMD ["npm", "start"]
