# Development Dockerfile for hot-reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Note: Dependencies are installed via docker-compose command
# to work properly with volume mounts

# Expose port
EXPOSE 3000

# Start development server (overridden by docker-compose)
CMD ["npm", "start"]
